language: python
python:
  - "3.4"
  - "3.3"
services:
  - mysql
addons:
  mariadb: '10.0'
before_install:
  - mysql -e "CREATE DATABASE IF NOT EXISTS self_todo" -u root
  - mysql -u root self_todo < dump.sql
dependencies:
  - pip install pytest
  - pip install oursql3
script: py.test
